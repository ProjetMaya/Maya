<div class="container-fluid contenu">
    {{ form_start(form) }}

    <div class="row">
        <div class="col-md">{{ form_row(form.email) }}</div>
    </div>

    <div>
        {{ form_row(form.roles, {label_attr: {class: 'checkbox-custom'}, attr: {class: 'list-group-item roles'}}) }}
    </div>

    {{ form_widget(form) }}
    <div class="row" style="text-align: center">
        <div class="col">
            <button class="btn btn-primary btn-xs"><i class="fa fa-save"></i> Sauvegarder</button>
        </div>
        <div class="col">
            <button class="btn btn-info btn-xs" type="reset" title="Effacer la saisie"><i class="fa fa-eraser"> Annuler</i></button>
        </div>
        {% if app.request.get('id') %}
            <div class="col">
                {{ include('user/_delete_form.html.twig') }}
            </div>
        {% endif %}
        <div class="col">
            <a href="{{ path('user_index')}}" class="btn btn-secondary btn-xs"><i class="fa fa-undo"></i> Retour liste</a></span></th>
        </div>
    </div>
    {{ form_end(form) }}
</div>

